# Images

Place browser-facing repository images here. For a custom brand image, add a
square PNG, JPG, or WebP file and set `repository.logoUrl` in
`data/packages.json` to its path, for example `assets/images/logo.png`.
